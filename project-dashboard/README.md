# GooseMed 開發看板

這是 GooseMed 的地端開發進度看板。看板只記錄程式修改、風險與驗證結果，不得填入病患資料。

```bash
cd project-dashboard
pnpm install
pnpm dev
```

開啟 `http://localhost:3000/`。拖曳或新增卡片後，內容會寫回 `data/board.json`，可由 Git 留下變更紀錄。
